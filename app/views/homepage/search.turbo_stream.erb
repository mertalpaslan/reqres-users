<%= turbo_stream.replace('users_list') { render User::ListComponent.new(collection: @collection) } %>
<%= turbo_stream.replace('pagination_links') { render User::PaginationComponent.new(collection: @collection) } %>
