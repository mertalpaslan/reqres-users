<%= turbo_stream.replace('pagination-links') { render User::PaginationComponent.new(collection: @collection) } %>
<%= turbo_stream.replace('users-list') { render User::ListComponent.new(collection: @collection) } %>
<%= turbo_stream.replace('search-field') { render User::SearchComponent.new(page: @collection.page) } %>
