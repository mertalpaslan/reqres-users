<div id="search-field">
  <%= form_with url: search_path(page: @page), class: "user-all-search", method: :post do |form| %>
    <span class="search-label">Search:</span>
    <%= form.search_field :search, oninput: "this.form.requestSubmit()" %>
  <% end %>
</div>
